# VM Wedding Movie Site

このZIPは最新版です。

- `index.html` は単体で動く最新版です
- Cloudflare Pages / GitHub Pages ではこの `index.html` をトップ階層に置いてください
- `assets` は画像素材の保管用です
- `styles.css` / `site.js` は確認・調整用です

## GitHubで差し替える時
既存の `index.html` をこのZIP内の `index.html` で必ず上書きしてください。
古い `hero-video-preview.html` は不要です。
